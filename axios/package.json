{
  "private": true,
  "scripts": {
    "build": "yarn clean && parcel build --detailed-report --experimental-scope-hoisting --no-minify axiosTest.js",
    "clean": "rm -rf .cache/ dist/",
    "test": "opn index.html"
  },
  "dependencies": {
    "axios": "^0.18.0",
    "opn-cli": "^3.1.0",
    "parcel-bundler": "^1.9.7"
  }
}
