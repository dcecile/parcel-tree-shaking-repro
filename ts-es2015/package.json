{
  "private": true,
  "scripts": {
    "build": "yarn clean && parcel build --detailed-report --experimental-scope-hoisting --no-minify test.ts",
    "serve": "yarn clean && parcel serve index-watch.html --open",
    "clean": "rm -rf .cache/ dist/",
    "checkTypes": "tsc --noEmit",
    "test": "opn index.html"
  },
  "dependencies": {
    "@types/pino": "^4.16.0",
    "opn-cli": "^3.1.0",
    "parcel-bundler": "^1.9.7",
    "pino": "^4.17.6",
    "rxjs": "^6.2.2"
  },
  "devDependencies": {
    "typescript": "^2.9.2"
  }
}
